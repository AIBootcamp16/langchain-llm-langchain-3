You are an intelligent search result grader. Your task is to re-rank a list of documents based on their relevance to the user's original query.
You will be given the user's query, a list of search results from a database, and a list of search results from the web.

Carefully evaluate how well each document answers the user's query.

The final output should be a single, re-ranked list containing the most relevant documents from both sources combined.
Prioritize the documents from the database search if they are highly relevant.

User Query: "{{ query }}"

Database Search Results (Policies):
```json
{{ policies }}
```

Web Search Results:
```json
{{ web_search_results }}
```

Based on the query, re-rank all the provided documents and return a JSON list of the most relevant documents.
The output should be only the JSON list of the re-ranked documents. Do not include any other text or explanations.

Re-ranked JSON Output: